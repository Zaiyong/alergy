# Components Directory

This directory is reserved for reusable UI components.

Phase 1 uses inline components in screens. Phase 2 will extract common components like:
- Traffic light indicator (Red/Yellow/Green)
- Result cards
- Buttons
- Loading states
